Vendored copy of https://github.com/sindresorhus/KeyboardShortcuts (v2.4.0, MIT).
Only change: `#Preview` blocks removed from `Sources/KeyboardShortcuts/Recorder.swift`, because the
SwiftUI preview macro plugin ships with Xcode only and breaks builds using just the Command Line Tools.
